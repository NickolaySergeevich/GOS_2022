<Window x:Class="GOS.Forms.MessageForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Внимание" Height="100" Width="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="{x:Null}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Label x:Name="label_warningText" x:FieldModifier="public" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
        <Button x:Name="button_exit" Grid.Row="1" Content="Exit" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="10, 2" Margin="10, 5" Click="Button_exit_OnClick"></Button>
    </Grid>
</Window>
